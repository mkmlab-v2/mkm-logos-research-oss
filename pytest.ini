[pytest]
# Workspace root: markers for dual-regime Fact-Lock tests (see projects/bitcoin-trading/tests/conftest.py)
markers =
    regime_integrity: Dual regime integrity / Fact-Lock tests (REGIME_INTEGRITY_REPORT.json)
    boundary_ssot: Thin SSOT JSON ↔ file linkage (no runtime engines)
    integration: Heavy or full-pipeline tests (optional; exclude with -m "not integration")

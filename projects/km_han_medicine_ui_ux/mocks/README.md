# Mocks — Antigravity / Figma drop zone

Place deliverables here after Pack E/F/G runs:

```
mocks/
  PackE-NationalAsk/     # .fig export or HTML
  PackF-Clinician/
  PackG-HubClinical/
  pack_a_km_ask_tokens/ # DTCG delta JSON if separate from ssot/
```

Empty until Antigravity completes. Cursor merge references these paths in PR description.

/**
 * Sidebar icon rail uses `HubPluginIcon` (SVG). This module keeps plugin id SSOT only.
 * @see projects/no1kmedi/src/components/shell/HubPluginIcon.tsx
 */
export type { UniverseHubPluginId } from "@/lib/universeHubPluginsV2";

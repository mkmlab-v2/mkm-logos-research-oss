# Logos mindmap viz archive

`LogosResearchSubgraphViz.tsx` (frozen force-graph, `fx/fy` fixed layout) was **retired from the studio UI** on 2026-06-26 in favor of **Explore mesh** (`LogosResearchExploreMeshPanel` + `@cosmos.gl/graph`).

- **Receipt mode** remains: `LogosResearchPathMindmapPanel` + SVG radial.
- **Explore mode** replaces the former "고급 감사" tab.
- Frozen viz source kept at `../LogosResearchSubgraphViz.tsx` for reference only.

---
title: Infra/GPU development lane (로컬 하드웨어 · ML 런타임)
alwaysApply: false
description: Infra/GPU·Ollama·로컬 PC 경로·GPU 번들 작업 시 — MISSION_LOG Infra 행 + LOCAL_MACHINE_POINTER만 선독. 사상 4AI 코어와 혼동 금지.
---

# Infra/GPU lane (Cursor 개발 레인 · Fact-Lock)

**Purpose:** 로컬 GPU·Ollama·드라이버·경로·`GpuRecommendedBundle` — **PC 스펙을 매 채팅 재조사하지 않도록** 디스크 포인터만 선로드한다.

**NOT this lane:** MKM **사상 4AI**(태양/소양/태음/소음)·`run_sasang_4agent_*` B-track 충돌 모델 — **별 격벽** (`mkm-cognitive-architecture-v1.mdc`).

## 재개 트리거 (동등)

「**Infra 레인**」「**GPU 레인**」「**Infra/GPU**」「미션로그 이어서 · Infra」「미션로그 이어서 · GPU」

## ALWAYS (첫 턴 · 다른 답변보다 먼저)

1. `MISSION_LOG.md` — 표 「다음 1타」에서 **`Infra/GPU` 행만** 읽는다(전체 핸드오프·ARCHIVE **열지 않음**).
2. `docs/final/LOCAL_MACHINE_POINTER_V1.md` — **있으면** Read(표만). **없으면** `docs/final/LOCAL_MACHINE_POINTER_V1.template.md` 절차만 안내하고 `ollama_models_dir`·`git_bare_primary` 등은 **「확인 필요」** — 추측 경로 금지.
3. 필요 시 **부분만:** `AGENTS.md` 「로컬 PC 전용 경로」·「【GPU 권장 번들】」·`CLAUDE.md` Ollama 한 줄.
4. 점검 요청 시: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Invoke-MkmPersonaHealth_v1.ps1 -Persona GpuRecommendedBundle` (exit code 보고).

## NEVER

- 사상 **4-Agent**·태양/소양/태음/소음을 **Infra 담당 AI**처럼 서술하거나 역할 합선.
- `LOCAL_MACHINE_POINTER`·`.env` 없이 GPU/모델 루트·호스트 경로 **단정**.
- `MISSION_LOG` 전체·`MISSION_LOG.old.md` 기본 로드(레인 행 초과).
- Track A·실매매·승격을 Infra 점검 exit 0만으로 **GO** 단정.

## OPTIONAL

- P0 경로만: `scripts\verify_p0_constitution_gate_paths.ps1`
- Control-Integrity smoke: `AGENTS.md` §1.2.1 · `-Persona P0` / `GpuRecommendedBundle`

---
title: MKM 자동화 게이트 — 앵커·NotebookLM·로컬 검증
alwaysApply: true
priority: 2
---

# MKM 자동화 게이트 (Fact-Lock 정렬)

**목적:** 브리핑·추측보다 **디스크 SSOT·exit code** 우선. **상위:** `.cursorrules` TITAN · Ask Gate.

## ALWAYS

- 구현·경로: `CONSTITUTION_INFERENCE_IMPLEMENTATION_FACTS.md` + 호출 가능 스크립트만 — 없으면 **미구현/확인 필요**.
- **Pre-answer / claim:** 가능·불가·완료 확정 전 `근거 경로 + 실행 맥락`(+exit code when run); 없으면 `확인 필요`/`추정`. Header `[스캔]→[리콜]→[실행]`. Split `manual_human_click` vs `automated_script_api` (예: 발송버튼클릭≠메일자동화 — 후자는 스크립트·로그·exit 필요).
- NotebookLM·외부 브리핑 = 참고만. NL MCP 필요 시 **1회 실제 호출** 후 판정; 미주입 시 터미널·Read 폴백 (`notebooklm-mcp-session-bridge.mdc`).
- 긴 외부 paste·완료/연동 주장·FAIL-COMP 합선 의심 → **트리거형** `@mkm-paste-epistemic-triage-v1` / `py scripts/run_mkm_paste_epistemic_triage_v1.py` (매턴 푸터 금지).
- 저위험 모호성 → 합리적 기본값·사후 1줄. 실거래·법규·zero_tolerance → 추론만으로 커밋 금지.
- **메타인지·평면·가시성:** 완료/상용/「보인다」 전 adversarial + `check_mkm_product_quality_truth_gate_v1` + `check_mkm_claim_method_visibility_honesty_v1` (또는 C6). `suspect_first` · harness/rev≠product · 「안 보임」=최고 반증.
- **Control hard gate:** 간단 확인 Q에 Task/explore 금지 · 채팅 영문 미완료표 금지 · 「하지 마」→forbid pin. 답 전 `py scripts/check_mkm_control_hard_gate_v1.py` (지휘관「하드게이트」시 의무). Soft `.mdc` alone ≠ obedience. `@mkm-control-hard-gate-v1`

## 묻기 전 (저위험)

P0 `verify_p0_constitution_gate_paths.ps1` · Git `Verify-GitWorkspaceSanity.ps1` · 예언 시 해당 pytest. 주간 `run_fact_lock_bundle.ps1`.

## NEVER

- NL 출력으로 규칙·스키마·「이미 구현」 단정.
- 검증 생략 후 추측 경로 선택 (불확실하면 **확인 필요**).
- Destiny / live Ask deploy without this-chat commander deploy ACK (`mkm-done-card-completion-v1`).
- Simple clarification → Task/explore / 「open 문맥 디스크 확인」 over-delegation.

## OPTIONAL

- Bio/DNA 승격: `reports/bio_dna_constitution_final_approval_latest.json` · human gate — bio 레인 작업 시만.
- CI tail: `CONSTITUTION` §6 포인터 + `dual-regime-integrity.yml` (표 1:1 강제 아님).

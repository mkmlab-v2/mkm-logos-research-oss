# Copy to /etc/mkm/compression-api.env on the VPS (chmod 600).
# Freemium: comma-separated tokens for enterprise (Track A). Public requests use Track B (literal KPI).
# Leave empty for legacy single-track behavior (all clients treated as enterprise).
# COMPRESSION_API_ENTERPRISE_KEYS=your-secret-token,another-token

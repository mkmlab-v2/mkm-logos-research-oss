{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mkm.local/schemas/showroom_topology_radar_snapshot_v1.schema.json",
  "title": "showroom_topology_radar_snapshot_v1",
  "description": "Read-only snapshot for Topology Radar / meaning-graph showroom surfaces. Track B outputs only; no trade signals.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "generated_at_utc",
    "stale_after_utc",
    "hypo_banner",
    "summary_one_line",
    "artifact_refs",
    "disclaimer_ref",
    "no_trade_signals"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "showroom_topology_radar_snapshot_v1"
    },
    "generated_at_utc": {
      "type": "string",
      "minLength": 10,
      "description": "ISO8601 UTC recommended (e.g. 2026-05-13T12:00:00Z)."
    },
    "stale_after_utc": {
      "type": "string",
      "minLength": 10,
      "description": "Do not present as fresh after this instant (align with public showroom delay policy)."
    },
    "hypo_banner": {
      "type": "string",
      "description": "Must remain visibly HYPO-tagged in UI (e.g. prefix [HYPO])."
    },
    "summary_one_line": {
      "type": "string",
      "maxLength": 500
    },
    "artifact_refs": {
      "type": "array",
      "minItems": 1,
      "maxItems": 32,
      "items": {
        "type": "string",
        "minLength": 4,
        "description": "Repo-relative or published artifact path strings only; no secrets."
      }
    },
    "disclaimer_ref": {
      "type": "string",
      "description": "Must align with jemaai.cloud showroom disclaimer IDs (e.g. jemaai_showroom_v1)."
    },
    "no_trade_signals": {
      "type": "boolean",
      "const": true
    }
  }
}

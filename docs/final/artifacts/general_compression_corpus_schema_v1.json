{
  "schema": "general_compression_corpus_schema_v1",
  "description": "Row contract for general-rail compression benchmark corpus.",
  "required_fields": {
    "raw_text": "string, source text to be compressed",
    "domain": "enum(meeting|policy_legal_lite|support_faq)",
    "sensitivity_level": "enum(low|medium|high)",
    "expected_key_terms": "array[string], must-preserve terms for integrity checks"
  },
  "optional_fields": {
    "doc_id": "string",
    "timestamp": "ISO-8601 UTC string",
    "language": "string (default: ko|en)",
    "task_label": "string (e.g. summarize, qa, classify)"
  },
  "validation_rules": [
    "raw_text must be non-empty",
    "expected_key_terms may be empty for low-sensitivity rows",
    "high sensitivity rows should include at least 1 expected_key_term"
  ]
}

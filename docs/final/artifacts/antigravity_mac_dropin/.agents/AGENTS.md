# MKM · Antigravity (Mac) — Design satellite only

**Role:** Visual draft · DTCG slots · HTML/Figma mocks.  
**Not:** Fact-Lock spine · pytest · ECC · live deploy · Cursor rules full stack.  
**Merge owner:** PC Cursor only. This agent never claims production merge.

**Device:** mkmlab MacBook — open a **thin design folder** only (not full monorepo).  
**SSOT (PC):** `docs/final/MACBOOK_OPT_OPS_CARD_V1.md` · Pack prompts under `docs/final/artifacts/jemaai_antigravity_design_prompt_packs_v1_latest.md`

---

## Always

1. One surface / one Pack per session (A → D → B/C, or KM E → F → G).
2. Fill `antigravity_proposed_*` / mocks only; keep existing class names and anchors.
3. Copy strings from attached `public-copy.json` / pack placeholders — no new marketing claims.
4. End with a **Cursor handoff** block: files touched · do-not list · suggested scoped CSS ≤ ~120 lines.

## Never

- Rewrite whole `globals.css` or invent parallel shell components.
- Change URL routing, intent routers, or Hub plugin logic.
- Put Track A KPI / compression headlines / live-trading copy in UI.
- Mix Hub gold/cream into Logos Ask, or Ask tokens into Hub, without an explicit Pack.
- Open or edit `.env`, secrets, patient data, or CONSTITUTION as law.
- Run Windows health scripts as “done” proof (gates run on PC Cursor).

---

## Surfaces (pick one)

| Pack | Surface | Anchors (restyle only) |
|------|---------|------------------------|
| A | Tokens | DTCG JSON slots only |
| B | `/hub` discover v3 | `UnifiedUniverseShellV2`, ask pill, sidebar, inspector |
| C | `/` marketing | existing sections; Ring 0 one-anchor hero |
| D | Trust pages | `/validation` `/safety` `/enterprise` chrome |
| E–G | KM ask / clinician / hub clinical | see KM Master Brief in thin repo |
| L-Ask | logos ask | scoped CSS + frozen class names only |

**Accent drift (do not collapse):** clinical green `#3d9b84` · SaaS blue `#5b8cff` · hub cream `#faf7f2` + gold `#c5a057`.

---

## Output contract

Prefer, in order:

1. Updated JSON (DTCG / seed slots) **or** mock HTML under `mocks/Pack*/`
2. Optional Figma link
3. Scoped CSS **diff** (not full file dump)
4. ≤1 page rationale (KO or EN) + **Do not** list

## Handoff footer (paste every turn)

```
CURSOR_HANDOFF
pack: <A|B|C|D|E|F|G|L-Ask>
files: <paths>
merge: scoped CSS / tokens only — PC Cursor
gates: PC only (design-tokens · marketing-copy · pytest as applicable)
send_gate: HOLD
```
